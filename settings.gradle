// 플러그인 파일을 프로젝트 내 .gradle 폴더에 저장
pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'ePAMS'

// 의존성 레포지토리 캐시를 프로젝트 폴더 내부로 설정
gradle.ext.gradleUserHome = "${rootDir}/.gradle"
