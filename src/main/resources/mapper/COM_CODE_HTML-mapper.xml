<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper SYSTEM "mybatis-3-mapper.dtd">


<mapper namespace="CodeHtmlMap">
     
    <!-- 모든 데이터 조회 -->
    <select id="findAll" resultType="codeHtmlMap">
        SELECT * FROM THURXE_CCDHTR
    </select>    
    
    <!-- 기본키 데이터 중복여부 조회 -->
    <select id="countById" parameterType="Long" resultType="codeHtmlMap">
        SELECT COUNT(*)
        FROM THURXE_CCDHTR WHERE HTML=#{HTML} AND CD_ID=#{CD_ID}
    </select>
    
    <!-- 신규 데이터 추가 --> 
    <insert id="insert" parameterType="codeHtmlMap">
        INSERT INTO THURXE_CCDHTR(HTML, CD_ID, CREATED_TIME)
            VALUES(#{HTML}, #{CD_ID}, SYSDATE)
    </insert>   
    
    <!-- 기존 데이터 삭제 --> 
    <delete id="delete" parameterType="codeHtmlMap">
        DELETE FROM THURXE_CCDHTR WHERE HTML=#{HTML} AND CD_ID=#{CD_ID}
    </delete>
    
        
</mapper>