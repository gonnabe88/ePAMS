<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper SYSTEM "mybatis-3-mapper.dtd">


<mapper namespace="LoginOTP">
     
    <!-- 모든 데이터 조회 -->
    <select id="findAll" resultType="loginOTP">
        SELECT * FROM THURXE_COTPIM
    </select>    
    
    <!-- 기본키 데이터 중복여부 조회 -->
    <select id="countById" parameterType="Long" resultType="loginOTP">
        SELECT COUNT(*)
        FROM THURXE_COTPIM WHERE SEQ_ID=#{SEQ_ID}
    </select>
    
    <!-- 신규 데이터 추가 --> 
    <insert id="insert" parameterType="loginOTP">
        INSERT INTO THURXE_COTPIM(SNO, OTP, MFA, CREATED_TIME)
            VALUES(#{EMP_NO}, #{OTP}, #{MFA}, SYSDATE)
    </insert>   
    
    <!-- 기존 데이터 삭제 --> 
    <delete id="delete" parameterType="loginOTP">
        DELETE FROM THURXE_COTPIM WHERE SEQ_ID=#{SEQ_ID}
    </delete>
    
    <!-- 기존 데이터 업데이트 --> 
    <update id="update" parameterType="loginOTP">
        UPDATE THURXE_COTPIM SET SNO=#{EMP_NO}, OTP=#{OTP}, MFA=#{MFA}, UPDATED_TIME=SYSDATE WHERE SEQ_ID=#{SEQ_ID}
    </update>
    
    <!-- 신규 데이터 추가 또는 기존 데이터 업데이트 -->
	<insert id="insertUpdate" parameterType="loginOTP">
	    INSERT INTO THURXE_COTPIM (SNO, OTP, MFA, CREATED_TIME)
	    VALUES(#{EMP_NO}, #{OTP}, #{MFA}, SYSDATE)
	    ON DUPLICATE KEY UPDATE
	    OTP = VALUES(OTP),
	    MFA = VALUES(MFA),
	    UPDATED_TIME = SYSDATE;
	</insert>
	
	    <!-- 모든 데이터 조회 -->
    <select id="findValidOneByUsername" parameterType="loginOTP" resultType="loginOTP">
    SELECT *
    FROM (
        SELECT * 
        FROM THURXE_COTPIM 
        WHERE SNO = #{EMP_NO}
          AND CREATED_TIME > SYSDATE - INTERVAL '3' MINUTE
        ORDER BY CREATED_TIME DESC
    )
    WHERE ROWNUM = 1
    </select> 
        
</mapper>