<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper SYSTEM "mybatis-3-mapper.dtd">


<mapper namespace="CodeHtmlDetail">
    
    <!-- html에 해당하는 Code 목록 조회 -->
    <select id="findAllByHtml" parameterType="String" resultType="CodeHtmlDetail">
        SELECT 
            CH.HTML_PTH_NM AS html, 
            H.HTML_NM      AS htmlName, 
            CH.CDVA_ID     AS code, 
            C.CDVA_NM      AS codeName 
        FROM 
            THURXE_CCDHTR CH, 
            THURXE_CCODEM C, 
            THURXE_CHTMLM H
        WHERE 
            CH.HTML_PTH_NM = H.HTML_PTH_NM AND
            CH.CDVA_ID     = C.CDVA_ID AND
            CH.HTML_PTH_NM = #{html}
    </select>    
    
</mapper>