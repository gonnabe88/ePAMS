<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CodeHtmlMap">

    <!-- Result Map 정의 -->
    <resultMap id="codeHtmlMap" type="codeHtmlDTO">
        <id property="html" column="HTML_FL_PTH"/>
        <id property="code" column="CDVA_ID"/>
        <result property="deleteYn" column="DEL_YN"/>
        <result property="guid" column="GUID"/>
        <result property="guidPrgSno" column="GUID_PRG_SNO"/>
        <result property="lstChgUsid" column="LST_CHG_USID"/>
        <result property="updatedTime" column="LST_CHG_DTM"/>
        <result property="createdTime" column="GNT_DTM"/>
    </resultMap>

    <!-- 모든 데이터 조회 -->
    <select id="findAll" resultMap="codeHtmlMap">
        SELECT * FROM THURXE_CCDHTA
    </select>

    <!-- 기본키 데이터 중복여부 조회 -->
    <select id="countById" parameterType="codeHtmlDTO" resultType="int">
        SELECT COUNT(*)
        FROM THURXE_CCDHTA
        WHERE HTML_FL_PTH = #{html} AND CDVA_ID = #{code}
    </select>

    <!-- 신규 데이터 추가 -->
    <insert id="insert" parameterType="codeHtmlDTO">
        INSERT INTO THURXE_CCDHTA (
            HTML_FL_PTH,
            CDVA_ID,
            DEL_YN,
            GUID,
            GUID_PRG_SNO,
            LST_CHG_USID,
            LST_CHG_DTM,
            GNT_DTM
        )
        VALUES (
            #{html},
            #{code},
            #{deleteYn},
            #{guid},
            #{guidPrgSno},
            #{lstChgUsid},
            SYSDATE,
            SYSDATE
        )
    </insert>

    <!-- 기존 데이터 삭제 -->
    <delete id="delete" parameterType="codeHtmlDTO">
        DELETE FROM THURXE_CCDHTA
        WHERE HTML_FL_PTH = #{html} AND CDVA_ID = #{code}
    </delete>

</mapper>
