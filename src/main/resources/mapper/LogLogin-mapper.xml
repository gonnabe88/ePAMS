<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "mybatis-3-mapper.dtd">

<mapper namespace="LogLogin">
    
    <!-- Result Map 정의
    <resultMap id="logLoginMap" type="logLogin">
        <id property="LGN_LOG_SNO" column="LGN_LOG_SNO"/>
        <result property="ENO" column="ENO"/>
        <result property="LGN_KD_NM" column="LGN_KD_NM"/>
        <result property="LGN_SCS_YN" column="LGN_SCS_YN"/>
        <result property="GNT_DTM" column="GNT_DTM"/>
    </resultMap> -->
    
    <!-- 모든 데이터 조회 -->
    <select id="findAll" resultType="logLogin">
        SELECT * FROM THURXE_CLOGNL
    </select>    
    
    <!-- 기본키 데이터 중복여부 조회 -->
    <select id="countById" parameterType="Long" resultType="logLogin">
        SELECT COUNT(*)
        FROM THURXE_CLOGNL 
        WHERE 
            LGN_LOG_SNO = #{LGN_LOG_SNO}
    </select>
    
    <!-- 신규 데이터 추가 --> 
    <insert id="insert" parameterType="logLogin">
        INSERT INTO THURXE_CLOGNL
        (
            ENO, 
            LGN_KD_NM, 
            LGN_SCS_YN, 
            GNT_DTM
        )
        VALUES(
            #{ENO},
            #{LGN_KD_NM},
            #{LGN_SCS_YN},
            SYSDATE
        )
    </insert>   
    
    <!-- 기존 데이터 삭제 (로그 테이블 특성상 불필요) --> 
    
    <!-- 기존 데이터 업데이트 (로그 테이블 특성상 불필요) -->

</mapper>