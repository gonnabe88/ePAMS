<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PreCheckProc">

    <select id="call" statementType="CALLABLE" parameterType="PreCheckProcDTO" resultType="string">
        {CALL OHR.PRE_CHECK_P(
            #{empId, mode=IN, jdbcType=NUMERIC},
            #{staYmd, mode=IN, jdbcType=DATE},
            #{endYmd, mode=IN, jdbcType=DATE},
            #{result, mode=OUT, jdbcType=VARCHAR}
        )}
    </select>

</mapper>