<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper SYSTEM "mybatis-3-mapper.dtd">

<mapper namespace="Board">
    <insert id="save" parameterType="board" useGeneratedKeys="true" keyProperty="id">
        insert into board(boardTitle, boardWriter, boardPass, boardContents, fileAttached)
            values(#{boardTitle}, #{boardWriter}, #{boardPass}, #{boardContents}, #{fileAttached})
    </insert>
    <select id="findAll" resultType="board">
        select id, boardTitle, boardWriter, boardHits, date_format(createdAt, "%Y-%m-%d") as createdAt
        from board order by id desc
    </select>
    <update id="updateHits" parameterType="Long">
        update board set boardHits=boardHits+1 where id=#{id}
    </update>
    <select id="findById" parameterType="Long" resultType="board">
        select id, boardTitle, boardWriter, boardPass, boardContents, boardHits,
        date_format(createdTime, "%Y-%m-%d %H:%i:%s") as createdTime, fileAttached
        from board where id=#{id}
    </select>
    <update id="update" parameterType="board">
        update board set boardTitle=#{boardTitle}, boardContents=#{boardContents} where id=#{id}
    </update>
    <delete id="delete" parameterType="Long">
        delete from board where id=#{id}
    </delete>
    <insert id="saveFile" parameterType="boardFile">
        insert into board_file(originalFileName, storedFileName, boardId)
        values (#{originalFileName}, #{storedFileName}, #{boardId})
    </insert>
    <select id="findFile" parameterType="Long" resultType="boardFile">
        select * from board_file where board_Id = #{id}
    </select>
    <delete id="initFile" parameterType="Long">
        delete from board_file where board_Id = #{id}
    </delete>
    <select id="findByFileId" parameterType="Long" resultType="boardFile">
        select * from board_file where id = #{id}
    </select>
    
    
    <insert id="insertBoardImage" parameterType="boardImage" useGeneratedKeys="true" keyProperty="id">
        insert into board_image(originalFileName, storedFileName)
            values(#{originalFileName}, #{storedFileName})
    </insert>
</mapper>