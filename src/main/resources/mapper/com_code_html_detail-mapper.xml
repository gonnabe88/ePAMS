<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper SYSTEM "mybatis-3-mapper.dtd">


<mapper namespace="CodeHtmlDetail">
     
    <!-- html에 해당하는 Code 목록 조회 -->
    <select id="findAllByHtml" parameterType="String" resultType="CodeHtmlDetail">
        SELECT 
        	CH.HTML   AS html, 
        	H.HTML_NM AS htmlName, 
        	CH.CD_ID     AS code, 
        	C.CD_NM   AS codeName 
        FROM com_code_html CH, com_code C, com_html H
        WHERE CH.HTML = H.HTML AND
        	  CH.CD_ID   = C.CD_ID AND
        	  CH.HTML = #{html}
    </select>    
        
</mapper>